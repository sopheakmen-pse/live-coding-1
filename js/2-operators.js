// Arithmetic operators +, -, *, /
const x = 100 + 200;
const y = 100 * 200;
console.log("x: ", x);
console.log("y: ", y);

// Assignment operators =, +=, -=, *=, /=

// Comparison operators ==, ===, !=, !==, <, >, <=, >=

// Logical operators &&, ||, !
